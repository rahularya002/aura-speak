/**
 * When `DATABASE_URL` is set, you can extend this project with `pg` + `pgvector`
 * and swap `lib/store/vectorStore.ts` for a Postgres-backed implementation.
 * The file-based store is the default and requires no database.
 */
export const PG_VECTOR_PLACEHOLDER = true;
